; Copy this file to secrets.ini and fill in the real value. secrets.ini is gitignored.
; OTA upload auth used by platformio.ini (${secrets.ota_password}) and flash_all.ps1.
; MUST match OTA_PASSWORD in src/secrets.h.
[secrets]
ota_password = CHANGE_ME
